"use strict";
var login = (function () {
    function login(id, title, auther, url) {
        this.id = id;
        this.title = title;
        this.auther = auther;
        this.url = url;
    }
    return login;
}());
exports.login = login;
//# sourceMappingURL=login.js.map